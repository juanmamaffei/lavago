<p>
    <%= if user_signed_in? then link_to('Cerrar sesión', destroy_user_session_path, method: :delete) else link_to('Iniciar sesión', new_user_session_path) end %>
</p>
<% if (@incomplete_profile == true) %>
  <%= react_component("Profile", @user) %>
<% else %>
  <ul>
      <li><%= link_to('Productos',products_path) %></li>
      <li><%= link_to('Lavanderías',laundries_path) %></li>
      <li><%= link_to('Suscripciones',suscriptions_path) %></li>
      <li><%= link_to('Pedidos',orders_path) %></li>
      <li><%= link_to('Cadetes',carriers_path) %></li>

  </ul>
<% end %>
Datos personales
<ul>
  <li>Email: <%= @user.email %></li>
  <li>Nombre: <%= @user.name %></li>
  <li>Apellido: <%= @user.last_name %></li>
  <li>Localidad: <%= @user.city %></li>
  <li>Dirección: <%= @user.address %></li>
  <li>Detalles de dirección: <%= @user.address_details %></li>
  <li>Puntaje por tus pedidos: <%= @user.score %></li>
 
</ul>
<script type="javascript">
// Define your product name and version
tomtom.setProductInfo('Codepen Examples', '4.46.3');
var map, marker;
// Setting TomTom keys
tomtom.key('6fEWMnnxWPDJ3BpQGKffuMUlyQiBlUlu');
tomtom.searchKey('6fEWMnnxWPDJ3BpQGKffuMUlyQiBlUlu');
//Creating the map
map = tomtom.map('map', {
    source: 'vector',
    basePath: 'https://api.tomtom.com/maps-sdk-js/4.46.3/examples/sdk'
}).setView([52.373154, 4.890659], 14);
// Adding marker to the map
marker = tomtom.L.marker([52.373154, 4.890659], {
    draggable: true
}).bindPopup('Amsterdam').addTo(map);
// Show popup each time the marker is moved
marker.on('dragend', function(e) {
    tomtom.reverseGeocode({position: e.target.getLatLng()})
        .go(function(response) {
            if (response && response.address && response.address.freeformAddress) {
                marker.setPopupContent(response.address.freeformAddress);
            } else {
                marker.setPopupContent('No results found');
            }
            marker.openPopup();
        });
});


 </script>
<div id="map" class='use-all-space'> </div>
<!--- MAPA
    <div id="map"></div>
    <script>
      // This example adds a user-editable rectangle to the map.
      function initMap() {
        var map = new google.maps.Map(document.getElementById('map'), {
          center: {lat: -32.961385, lng: -60.649651},
          zoom: 14
        });

        var myLatLng = {lat: -32.961385, lng: -60.649651};

        var map = new google.maps.Map(document.getElementById('map'), {
          zoom: 14,
          center: myLatLng
        });

        var marker = new google.maps.Marker({
          position: myLatLng,
          map: map,
          title: 'Hello World!'
        });

        var bounds = [
            {lat: -32.961185, lng: -60.649151},
            {lat: -32.961185, lng: -60.649951},
            {lat: -32.961485, lng: -60.649551},
            {lat: -32.961485, lng: -60.649751}
        ];

        // Define a rectangle and set its editable property to true.
        var rectangle = new google.maps.Polygon({
          paths: bounds,
          editable: true
        });
        rectangle.setMap(map);
      }

    </script>
    <script async defer
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAP9BJ5wfeQ23i_gVQe0tbczeHcIcKNLWQ&callback=initMap">
    </script>
--->